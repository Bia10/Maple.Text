{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "test with coverage",
      "type": "shell",
      "command": "New-Item -Path TestResults -ItemType Directory -Force | Out-Null; dotnet run --project src/Maple.Text.Test/Maple.Text.Test.csproj --configuration Debug -- --coverage --coverage-output-format cobertura --coverage-output \"$PWD/TestResults/coverage.cobertura.xml\"",
      "group": {
        "kind": "test",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "problemMatcher": []
    }
  ]
}
